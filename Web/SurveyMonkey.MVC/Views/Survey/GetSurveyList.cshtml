@model IEnumerable<SurveyMonkey.DataTransferObject.Response.SurveyListResponse>

@{
    ViewData["Title"] = "GetSurveyList";
}


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
            <th>durum</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    
        <tr> 
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="report" asp-route-id="@item.Id">detay</a>
            </td>
            @if (item.ExpireDate<DateTime.Now)
                {
                    <td>
                        anket süresi doldu.
                    </td>
                }
                else
                {
                    <td>
                        @item.ExpireDate
                    </td>
                }
        </tr>
}
    </tbody>
</table>

